<UserControl x:Class="Controls.Drawer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Name="root">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button x:Name="paintBtn" Click="paintBtn_Click">&#xe104;</Button>
                <Button x:Name="clearBtn" Click="clearBtn_Click">&#xe106;</Button>
            </StackPanel>
            <InkCanvas Background="{Binding ElementName=root, Path=Background}" x:Name="canvas"/>
        </DockPanel>
    </Grid>
</UserControl>
